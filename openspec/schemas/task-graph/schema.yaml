name: task-graph
version: 1
description: Default OpenSpec workflow - proposal → specs → design → tasks
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      Create the proposal document that establishes WHY this change is needed.

      Sections:
      - **Why**: 1-2 sentences on the problem or opportunity. What problem does this solve? Why now?
      - **What Changes**: Bullet list of changes. Be specific about new capabilities, modifications, or removals. Mark breaking changes with **BREAKING**.
      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<capability-path>/spec.md`. Use kebab-case for path segments you introduce (e.g., `user-auth` or `identity/user-auth`) and follow the project's existing spec organization.
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behavior changes (not just implementation details). Each needs a delta spec file. Use the exact existing path under `openspec/specs/`. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.

      IMPORTANT: The Capabilities section is critical. It creates the contract between
      proposal and specs phases. Research existing specs before filling this in.
      Each capability listed here will need a corresponding spec file.

      Every change must either declare at least one capability (new or
      modified) or explicitly opt out of specs: `openspec validate` rejects a
      change with zero deltas unless the change's `.openspec.yaml` sets
      `skip_specs: true`. Use `skip_specs: true` only when no spec-level
      behavior changes (pure refactor, tooling, docs) - specs describe
      behavior, so if behavior does not change, no spec should change either.
      Do not invent a requirement just to satisfy validation.

      Keep it concise (1-2 pages). Focus on the "why" not the "how" -
      implementation details belong in design.md.

      This is the foundation - specs, design, and tasks all build on this.
    requires: []

  - id: specs
    generates: "specs/**/*.md"
    description: Detailed specifications for the change
    template: spec.md
    instruction: |
      Create specification files that define WHAT the system should do.

      A spec is a behavior contract, not an implementation plan.

      Good spec content:
      - Observable behavior users or downstream systems rely on
      - Inputs, outputs, and error conditions
      - External constraints (security, privacy, reliability, compatibility)
      - Scenarios that can be tested or explicitly validated

      Avoid in specs:
      - Internal class/function names
      - Library or framework choices
      - Step-by-step implementation details
      - Detailed execution plans (those belong in design.md or tasks.md)

      Quick test: if the implementation can change without changing externally
      visible behavior, it likely does not belong in the spec.

      Create one spec file per capability listed in the proposal's Capabilities section.
      `<capability-path>` is the spec directory relative to `specs/` (for example,
      `user-auth` or `identity/user-auth`). Preserve the full path:
      - New capabilities: use the exact path from the proposal at `specs/<capability-path>/spec.md`. Any path segment newly introduced in the proposal must be kebab-case. Follow the project's existing organization; do not add a new domain level when the project uses a flat layout.
      - Modified capabilities: use the exact existing path from `openspec/specs/<capability-path>/` when creating the delta at `specs/<capability-path>/spec.md`. Do not move or rename the capability.

      There must be at least one spec file unless the change's `.openspec.yaml`
      sets `skip_specs: true` (no spec-level behavior change) - `openspec validate`
      rejects a zero-delta change without that marker. If the proposal lists no
      capabilities and `skip_specs` is not set, revisit the proposal first.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed behavior - MUST include full updated content
      - **REMOVED Requirements**: Deprecated features - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.

      New capabilities only: start the delta spec with a `## Purpose` section -
      one or two sentences (50+ characters, or `openspec validate --strict`
      reports it as too brief) describing what the capability is for. Archive
      copies it into the main spec it creates; without it the new main spec is
      left with a `TBD ... Update Purpose after archive` placeholder to fill in
      by hand. Do NOT add `## Purpose` to a delta for an existing capability -
      that spec already has one and the delta's is ignored. To change an
      existing capability's Purpose - including a leftover `TBD` placeholder -
      edit `<planningHome.root>/openspec/specs/<capability-path>/spec.md`
      directly. `planningHome.root` comes from the `openspec instructions ...
      --json` response. Always use it rather than a repo-relative path: it
      resolves to the store whenever the change lives in one - whether that
      came from `--store`, a project `store:` pointer, or a global default
      store - and to the current repository otherwise. Do not try to work out
      which case applies; the field already has.

      MODIFIED requirements workflow:
      1. Locate the existing requirement in `<planningHome.root>/openspec/specs/<capability-path>/spec.md` (the same store-aware root as above)
      2. Copy the ENTIRE requirement block (from `### Requirement:` through all scenarios)
      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior
      4. Ensure header text matches exactly (whitespace-insensitive)

      Common pitfall: Using MODIFIED with partial content loses detail at archive time.
      If adding new concerns without changing existing behavior, use ADDED instead.

      Example (a new capability, so it opens with `## Purpose`):
      ```
      ## Purpose

      Lets users take their data out of the product in a portable format.

      ## ADDED Requirements

      ### Requirement: User can export data
      The system SHALL allow users to export their data in CSV format.

      #### Scenario: Successful export
      - **WHEN** user clicks "Export" button
      - **THEN** system downloads a CSV file with all user data

      ## REMOVED Requirements

      ### Requirement: Legacy export
      **Reason**: Replaced by new export system
      **Migration**: Use new export endpoint at /api/v2/export
      ```

      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal

  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: |
      Create the design document that explains HOW to implement the change.

      When to include design.md (create only if any apply):
      - Cross-cutting change (multiple services/modules) or new architectural pattern
      - New external dependency or significant data model changes
      - Security, performance, or migration complexity
      - Ambiguity that benefits from technical decisions before coding

      Sections:
      - **Context**: Only the current state and constraints needed to explain the approach. Reference the proposal for motivation instead of restating it (e.g., "See proposal.md - Why").
      - **Goals / Non-Goals**: What this design achieves and explicitly excludes. Don't restate the proposal's scope - add only design-level boundaries.
      - **Decisions**: Key technical choices with rationale (why X over Y?). Include alternatives considered for each decision.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)
      - **Open Questions**: Unknowns that can safely be answered later without
        changing the specs, the approach, or the task breakdown. Omit if none.

      Open questions are for genuinely deferrable unknowns, not decisions you
      skipped. If a question would change the specs, the chosen approach, or
      the task breakdown, resolve it now - ask the user instead of guessing.

      Focus on architecture and approach, not line-by-line implementation.
      The proposal covers why and what; design covers how. Reference the
      proposal for motivation and, once written, the specs for requirements -
      if a section would only restate them, point to them instead.

      Good design docs explain the "why" behind technical decisions.
    requires:
      - proposal

  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks, dependencies, difficulty, and verification
    template: tasks.md
    instruction: |
      Create the task list that breaks down the implementation work.

      Before writing tasks, check design.md for Open Questions. If any of them
      would change what gets built, resolve them with the user first - do not
      bake an unstated assumption into the task list.

      **IMPORTANT: Follow the template below exactly, including the metadata
      suffix on every task line.** The apply phase only parses the leading
      `- [ ]`/`- [x]` to track progress, so the metadata after the
      description does not interfere with it - but a companion visualizer
      parses that metadata separately to render a real dependency graph, so
      it MUST be present and MUST follow the exact `key: value` format below,
      in this order, separated by ` | `. Tasks not using `- [ ]` won't be
      tracked by either.

      **Task line format (all seven fields required, in this order):**
      ```
      - [ ] X.Y Task description | depends: <refs> | difficulty: <level> | verify: <check> | status: <state> | files: <paths> | generate: <command>
      ```
      - `depends`: comma-separated task numbers this task needs finished first
        (e.g. `1.1,1.2`), or `none` if it has no prerequisite within this task
        list. Reference only tasks defined earlier in this same file.
      - `difficulty`: exactly one of `easy`, `medium`, `hard` - your honest
        estimate of implementation effort/risk for that specific task, not
        the change as a whole.
      - `verify`: how to confirm the task is actually done - a concrete test
        command, an explicit manual check, or an observable behavior. Not a
        restatement of the task description.
      - `status`: always `not_started` at creation time - the apply phase is
        responsible for advancing this as work happens (see apply.instruction
        below). Never write anything else here yourself.
      - `files`: always `none` at creation time - same reasoning, the apply
        phase fills this in as files are actually produced.
      - `generate`: `none` for ordinary code/config/test/doc tasks. For a
        task whose entire deliverable is a generated game-art asset (a
        sprite, icon, tile, or background - not code that uses one), the
        exact command to produce it:
        `image_generation/venv/bin/python image_generation/cli.py "<prompt>" --mode pixel_art --task-file <this file's path> --task-id <this task's id>`
        (add `--mode image` instead of `pixel_art` for a non-pixel-art
        image). Write a real, specific prompt - not a placeholder. The
        `--task-file`/`--task-id` flags matter: the command updates this
        exact task's `files` and `status` fields itself when it finishes, so
        the executing agent does not need to hand-edit that metadata. The
        whole task line is split on ` | `, so the command (and the prompt
        inside it) must not itself contain a `|` character - rephrase the
        prompt instead of using a literal pipe.

      Guidelines:
      - Group related tasks under ## numbered headings
      - Each task MUST be a checkbox with the full metadata suffix above
      - Tasks should be small enough to complete in one session
      - Order tasks by dependency (what must be done first?) - `depends`
        should agree with the ordering, but state it explicitly regardless;
        don't make the reader infer it from position alone
      - A task with no real prerequisite still needs `depends: none` written
        out - never omit the field

      Example:
      ```
      ## 1. Setup

      - [ ] 1.1 Create new module structure | depends: none | difficulty: easy | verify: expected files are present | status: not_started | files: none | generate: none
      - [ ] 1.2 Add dependencies to package.json | depends: 1.1 | difficulty: easy | verify: package installation succeeds | status: not_started | files: none | generate: none

      ## 2. Core Implementation

      - [ ] 2.1 Implement data export function | depends: 1.2 | difficulty: medium | verify: export test passes | status: not_started | files: none | generate: none
      - [ ] 2.2 Add CSV formatting utilities | depends: 2.1 | difficulty: easy | verify: unit tests cover quoting and delimiters | status: not_started | files: none | generate: none

      ## 3. Art (only when the change actually needs generated game art)

      - [ ] 3.1 Generate player sprite icon | depends: none | difficulty: easy | verify: PNG exists, reads as a readable pixel-art character | status: not_started | files: none | generate: image_generation/venv/bin/python image_generation/cli.py "16-bit JRPG hero, pixel art, white background" --mode pixel_art --transparent --task-file openspec/changes/<this-change>/tasks.md --task-id 3.1
      ```

      Reference specs for what needs to be built, design for how to build it.
    requires:
      - specs
      - design

apply:
  requires: [ tasks ]
  tracks: tasks.md
  instruction: |
    Read context files, then work through pending tasks (top to bottom,
    respecting `depends`) one at a time. A companion visualizer renders each
    task's `status` and `files` field live, so update tasks.md as you go -
    not only at the very end:

    1. Before starting a task, edit its line in place: `status: not_started`
       → `status: in_progress`.
    2. If the task's `generate` field is not `none`, run that exact command
       (it already targets this tasks.md and this task's id) instead of
       writing code for it - it updates the task's own `files` and `status`
       fields itself when it finishes. Do not also hand-edit those two
       fields for this task; just verify the command succeeded.
    3. Otherwise, as you create or modify a file for the task, add its path
       to the task's `files` field (comma-separated, relative to the
       project root, e.g. `files: src/api.ts,src/api.test.ts`). Append to
       the existing list rather than overwriting it if the task already has
       files recorded.
    4. Once the implementation looks done but you have not yet run its
       `verify` check, set `status: in_review` (the `generate` command
       already does this for its own task).
    5. Run the `verify` check. If it passes, flip the checkbox to `[x]` and
       set `status: done`. If it fails, keep working on the task - do not
       mark it done or move to a dependent task.

    Pause if you hit blockers or need clarification - leave the task at
    whatever status is accurate (`in_progress` or `in_review`), do not mark
    it `done` on a guess.
